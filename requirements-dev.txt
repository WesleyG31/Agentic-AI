# Kompass — dev, test & evaluation dependencies.
# Install:  python -m pip install -r requirements-dev.txt
# (this also pulls in the runtime requirements below)

-r requirements.txt

# ── Testing & quality ─────────────────────────────────────────────────
pytest>=8.3
pytest-asyncio>=0.24
ruff>=0.6

# ── Evaluation harness ────────────────────────────────────────────────
# Metrics are computed by a transparent LLM-as-judge rubric (evals/judge.py)
# rather than the ragas package, which is incompatible with langchain 1.x.
# No extra dependencies needed beyond the runtime set.

# ── Framework comparison spike (Tier 2/3, optional) ───────────────────
# pydantic-ai>=0.0.14
# openai-agents>=0.0.1
