# Kompass — runtime dependencies (Python 3.11+).
# Install:  python -m pip install -r requirements.txt
# Pinned to floors known-good as of 2026-01.

# ── Agent framework (LangGraph v1) ────────────────────────────────────
langgraph>=1.0.0
langchain>=1.0.0
langchain-core>=1.0.0
langchain-openai>=0.3.0
langgraph-checkpoint-sqlite>=2.0.0
langchain-mcp-adapters>=0.1.0

# ── Config & typed outputs ────────────────────────────────────────────
pydantic>=2.9
pydantic-settings>=2.5
python-dotenv>=1.0

# ── Retrieval (local: Chroma + BM25; no Docker needed) ────────────────
chromadb>=0.5.5
rank-bm25>=0.2.2
networkx>=3.3

# ── Serving + UI ──────────────────────────────────────────────────────
fastapi>=0.115
uvicorn[standard]>=0.30
streamlit>=1.38
httpx>=0.27

# ── Model Context Protocol (MCP tool servers) ─────────────────────────
mcp>=1.0.0

# ── Optional infra (uncomment if you run docker-compose) ──────────────
# langgraph-checkpoint-postgres>=2.0.0   # durable Postgres checkpointer
# psycopg[binary]>=3.2
# langfuse>=2.50                          # self-hosted observability
